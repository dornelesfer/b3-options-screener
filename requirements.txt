# App + nightly refresh (deployment). torch NOT needed — cVolGAN scoring is
# pure numpy; torch was only used by the legacy training scripts.
#
# Upper bounds are deliberate: both GitHub Actions and Streamlit Cloud install
# fresh every run, so an unbounded major release can break the deploy overnight
# (pandas 3 did exactly that on 2026-08-09). Bump them on purpose, with a test.
streamlit>=1.32,<2
plotly>=5.18,<7
pandas>=2.0,<4
numpy>=1.24,<3
scipy>=1.10,<2
pyarrow>=14.0,<26
matplotlib>=3.7,<4
